import React, { Component } from "react";
import { get } from "../../utilities";

import "./WikiPageTableRow.css";

class WikiPageTableRow extends Component {
  constructor(props) {
    super(props);
    this.state = {
    };
  }

  componentDidMount() {
  }

  render() {
    return (
      <></>
    );
  }
}

export default WikiPageTableRow;
